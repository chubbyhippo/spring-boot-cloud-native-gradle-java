apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-postgres
  labels:
    db: example-postgres
spec:
  selector:
    matchLabels:
      db: example-postgres
  template:
    metadata:
      labels:
        db: example-postgres
    spec:
      containers:
        - name: example-postgres
          image: postgres:14.4
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              value: password
            - name: POSTGRES_DB
              value: exampledb_catalog
          resources:
            requests:
              cpu: 100m
              memory: 60Mi
            limits:
              cpu: 200m
              memory: 120Mi

---

apiVersion: v1
kind: Service
metadata:
  name: example-postgres
  labels:
    db: example-postgres
spec:
  type: ClusterIP
  selector:
    db: example-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432